console.log('Lodash is loaded:', typeof _ !== 'undefined');
var player = [];
var deck = [];
var counter = 0;
for (var i = 0; i < 4; i++) {
  player.push({
    name: null,
    hand: null
  });
}
deck.push(1);
counter.push(1);
